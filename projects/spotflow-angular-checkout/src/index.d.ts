interface Window {
  SpotflowCheckout: {
    CheckoutForm: any;
  };
}

declare var SpotflowCheckout: {
  CheckoutForm: any;
};

declare global {
  interface Window {
    SpotflowCheckout: {
      CheckoutForm: any;
    };
  }
}
